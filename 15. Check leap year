15. Check leap year 

import calendar # no any mathakut and direct import library!
year = int(input("Enter any year like(2027): "))

if calendar.isleap(year): # isleap is clarify(the year) using library.
  print("leap year!")
else:
  print("not leap year!")

# print(year)
print(year,"🤷")
